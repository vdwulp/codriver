% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/configure.R
\name{codriver_configure}
\alias{codriver_configure}
\title{Configure codriver}
\usage{
codriver_configure(name, ...)
}
\arguments{
\item{name}{Character. Provider name passed to \code{\link[ellmer:chat]{ellmer::chat()}}, in the form
\code{"provider"} or \code{"provider/model"}. Supported providers are listed on
\url{https://ellmer.tidyverse.org/index.html#providers}.}

\item{...}{Named arguments passed to the ellmer constructor, such as
\code{model} or \code{base_url}.}
}
\value{
Returns \code{TRUE} invisibly on successful provider configuration.
Returns \code{FALSE} invisibly if RStudio is not available or provider
validation fails.
}
\description{
Set up the AI provider that codriver uses to generate and edit R code and
comments. The configuration is saved locally and validated immediately by
making a test call to the provider.
}
\details{
Pass any arguments supported by the underlying \code{\link[ellmer:chat]{ellmer::chat()}} constructor
directly via \code{name} and \code{...}.

If no keyboard shortcut is registered for codriver, the function offers to
register the default shortcut \verb{Ctrl+/}. Shortcut registration is strongly
recommended but can be skipped or changed later via \code{Tools} >
\verb{Modify Keyboard Shortcuts} in RStudio.
}
\examples{
\dontrun{
  codriver_configure("openai", model = "gpt-4o")
  codriver_configure("openai_compatible/mistral", base_url = "http://localhost:1234/v1")
}

}
\seealso{
\itemize{
\item \code{vignette("getting-started-with-codriver")} for extended information on
configuring codriver
\item \code{vignette("start-using-codriver")} for an overview of modes and result
presentation
\item \code{\link[ellmer:chat]{ellmer::chat()}} and supported providers at
\url{https://ellmer.tidyverse.org/index.html#providers}
}
}
